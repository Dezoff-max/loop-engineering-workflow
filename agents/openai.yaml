interface:
  display_name: "Loop"
  short_description: "Template-backed verified project loop"
  default_prompt: "Use $loop to set up, continue, audit, repair, build a verification matrix, or run a doctor check for this project."
